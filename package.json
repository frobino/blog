{
  "name": "eleventy-blog",
  "version": "1.0.0",
  "description": "Simple blog with 11ty",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx eleventy --formats=md,html,png",
    "serve": "npx eleventy --serve --formats=md,html,png",
    "debug": "DEBUG=* npx eleventy"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.10.0"
  }
}
